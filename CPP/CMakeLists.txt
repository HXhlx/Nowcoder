aux_source_directory(. cpp)
foreach(T_FILE_NAME ${cpp})
    string(REGEX REPLACE "\./(.+)\.cpp" "\\1" EXECUTABLE_NAME ${T_FILE_NAME})
    add_executable(${EXECUTABLE_NAME} ${T_FILE_NAME})
endforeach()